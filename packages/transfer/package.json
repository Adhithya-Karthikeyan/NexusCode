{
  "name": "@nexuscode/transfer",
  "version": "0.1.0",
  "description": "NexusCode Zero-Loss Context Transfer System (ZLCTS) — a harness-owned, provider-neutral knowledge core so switching LLM providers loses virtually no understanding.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@nexuscode/config": "*",
    "@nexuscode/shared": "*"
  }
}