{
  "name": "@nexuscode/tools-web",
  "version": "0.1.0",
  "description": "NexusCode web tool group — web_search (pluggable SearchProvider seam with a deterministic mock + lazy real provider), web_fetch (native fetch + HTML-to-text extraction + size/timeout caps + SSRF guard), and web_crawl (bounded same-origin BFS). Implements the @nexuscode/tools Tool contract; permission class: network.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@nexuscode/shared": "*",
    "@nexuscode/tools": "*"
  }
}
